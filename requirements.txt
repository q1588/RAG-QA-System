# ===== 基础依赖（本地开发 + Docker 通用）=====
fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
aiosqlite>=0.20            # SQLite 异步驱动（本地默认库）
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9    # 文件上传 / OAuth2PasswordRequestForm
python-dotenv>=1.0
bcrypt>=4.1                # 密码加盐哈希（passlib 与 bcrypt 5.x 不兼容，勿用）
PyJWT>=2.8                 # JWT 令牌
pypdf>=4.0                 # PDF 文档解析
python-docx>=1.1           # docx 文档解析
httpx>=0.27                # 冒烟测试客户端

# ===== LangChain 1.x + RAG =====
langchain>=1.0
langchain-core>=1.0
langchain-chroma>=1.0
langchain-ollama>=1.0
langchain-text-splitters>=1.0
chromadb>=1.5
